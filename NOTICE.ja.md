**他の言語で読む:** [简体中文](NOTICE.md)

> この文書は NOTICE.md（簡体字中国語）の日本語訳です。訳と原文の意味が異なる場合は、原文が優先されます。

# ZCode の関連機能についての説明と第三者コンポーネントに関する表示

この表示は、このリポジトリで公開しているソースコードと、そこから作られたビルド成果物に適用されます。実行形態ごとに、機能・権限・保存場所・ネットワーク上の動作が異なります。ある一つの形態の既定値を、プロジェクト全体に共通する設定と理解してはいけません。

## 一、AI の出力、実行権限、自動化に伴うリスク

AI が生成したコード、コマンド、説明、ファイル、提案には、誤り、抜け、セキュリティ上の欠陥、または第三者の権利に関する問題が含まれている可能性があります。実際のファイル、コマンドの終了ステータス、ツールの結果、テストと照らし合わせて確認してください。モデルが「完了した」「安全に処理した」「許可を得た」と述べても、それは事実の証明にも、許可の証明にもなりません。重要な操作の前には、実行対象、受け取る相手、影響を確認し、元に戻せるバックアップを残してください。

ファイル、ターミナル、Git、Agent ツール、外部プロセスは、それらを実際に動かしている OS アカウントの権限の範囲内で、ファイルの読み書き、プロセスの起動、ネットワークへのアクセスを行えます。Web またはリモートのワークスペースでは、操作がサーバー側、SSH ホスト、WSL、コンテナ環境で行われることがあります。いま画面を表示している機器だけを見て、実行場所を判断することはできません。現在の共有 Agent 実行アダプタは、既定の OS サンドボックスを提供していません。作業ディレクトリ、ワークスペースの識別情報、Git worktree、ブラウザのページ分離、Node REPL の実行コンテキストは、どれもすべてのツールに対するシステムレベルの分離の保証とみなしてはいけません。

共有の実行設定は、既定で `build` 権限モードを使います。独立した CLI が `--prompt` で非対話のタスクを実行するとき、`--mode` を指定しなければ `yolo` を使います。対話セッション、デスクトップのセッション、再開したタスク、ホストは、それぞれの設定、保存された値、または渡されたモードを使うことがあります。操作ごとに確認を求めるかどうかは、ツールの宣言、権限ルール、実行モードによって決まります。読み取り専用の計画の制約を同時に有効にしていない `yolo` は、通常のツール操作を許可します。対話型のツールと、確認を明示的に求めるツールには、それぞれ別のルールがあります。モデルのツール承認は、アプリ全体に共通する権限の切り替えではありません。これを根拠に、ターミナル操作、プラグインのプロセス、更新のダウンロードがすべて同じ承認の手順を通ると推定することはできません。詳しくは [CLI のプロンプトモード](apps/zcode-cli/packages/cli/src/run.ts#L486)と[ツールの権限判定](apps/zcode-cli/packages/core/src/permission/service.ts#L97)を参照してください。

| 機能または業務フック | 起こりうる影響とその範囲 |
| --- | --- |
| ファイル、Git、ターミナル、Shell、Node REPL | データの読み書きや削除、プログラムの実行、ネットワークへのアクセスができます。Git、パッケージマネージャ、ユーザーのコマンドは、それぞれ自身のスクリプトやフックを起動することもあります。タスクのスナップショット、Git のチェックポイント、セッションの再開は、ローカルのすべてのファイル、データベース、外部サービスのバックアップの代わりにはなりません。 |
| ユーザーとワークスペースの指示、スキル | ワークスペースの指示ファイル、スキルの文章、テンプレート、スクリプトは、モデルの計画に影響します。それらを読み込む、インストールする、表示することは、内容が信頼できる、合法である、または公式の安全審査を受けていることを意味しません。 |
| ライフサイクル Hooks | `SessionStart`、`UserPromptSubmit`、`PreToolUse`、`PermissionRequest`、`PostToolUse`、`PostToolUseFailure`、`Stop` は、コマンドやプロセスを起動できます。入力には、プロンプト、作業パス、ツールの引数、ツールの結果、応答が含まれることがあります。Hook は、コンテキストを追加する、ツールの入力を書き換える、権限の判断に加わる、ということもできます。 |
| ワークスペース Hook の信頼 | ワークスペース Hook は、ワークスペースの識別情報と宣言の要約（ダイジェスト）に基づいて審査します。宣言が変わると、それまでの信頼は無効になります。この仕組みは、プラグイン Hook、MCP 設定、その他のプロジェクトのスクリプトをまとめて審査するものではありません。 |
| プラグインと MCP | プラグインを有効にすると、自動で実行される Hook、ローカルのプログラム、リモートのツールが持ち込まれることがあります。現在の Agent 実行設定では、ワークスペース単位の MCP を自動接続の対象に含めています。MCP を有効にしてランタイムを起動すると、設定にあるコマンド、環境変数、認証ヘッダー、または OAuth を使ってサービスに接続することがあります。プラグイン Hook とワークスペース Hook は、別々の受け入れ手順を通ります。 |
| 起動スクリプトとインストールスクリプト | デスクトップやターミナルは、Shell の環境を取得するために、ログイン Shell とその起動設定を実行することがあります。依存関係のインストールでも、ネイティブモジュールのビルドなどのライフサイクルスクリプトが動くことがあります。これらのスクリプトはモデルのタスクより先に動くことがあります。モデルが一つずつ承認したコマンドだけが実行される、と理解してはいけません。 |
| サブエージェント、ワークフロー、バックグラウンドタスク | モデルを呼び出し続け、コマンドを実行し、費用や外部への副作用を生じることがあります。セッションを止める、ページを切り替える、ある要求を取り消す、といった操作をしても、すでに完了したファイルの書き込み、公開、アップロード、その他の外部への操作は取り消せません。 |
| デスクトップの定時タスクと空き時間タスク | スケジューラと実行ホストが使えるときに、保存された設定に従って動きます。ウインドウを隠したあとも実行を続けることがあります。ウインドウを閉じても、バックグラウンドやトレイに隠れるだけのことがあります。明示的な終了、スリープ、ネットワークの切断、アカウントやモデルが使えないことは、どれも実行に影響することがあります。電源を切ったあとも動き続けることや、実行されなかったタスクが必ずあとで実行されることは保証しません。独立した CLI は、Cron ツールを含んでいるからといって、永続的なスケジュール実行のサービスを自動で持つわけではありません。 |
| 内蔵ブラウザと Browser Use | Web ページにアクセスし、ページの内容を読み取り、画面を撮影または録画し、クリック、入力、アップロード、ダウンロードを行えます。ユーザーの操作で取り込んだり再利用したりしたブラウザのログイン状態によって、個人のデータにアクセスする、フォームを送信する、リモートのアカウントの内容を変える、ということが起こりえます。Web ページにアクセスするだけでも、そのサイトと、そのサイトが読み込むサービスとの通信が生じます。 |
| Computer Use | このリポジトリに付属する Computer Use パッケージは、使用できない仮の実装です。呼び出すと「使用できない」というエラーを返します。システムの画面撮影や操作の機能は提供しません。詳しくは [Computer Use の入口](packages/zcode-cua/index.js#L3)を参照してください。 |
| Web サービスとリモートの実行環境 | ネットワークで到達できる範囲、ログイン認証、プロセスのアカウント、ファイルの権限は、実際の配置（デプロイ）の内容によって決まります。認証の既定値は、起動の入口と待ち受けるアドレスによって異なります。ローカルでの待ち受け、接続元の確認、SSH の通信路は、それぞれ別の範囲を管理しています。開発用のサービスや、ローカルのコアのポートを、そのまま外部に公開してよいサービスとみなしてはいけません。 |
| コンテンツの共有、取り込み、リソースのプレビュー | 会話、コード、添付ファイル、生成物を読み取ったり送ったりすることがあります。取り込んだ共有コンテキスト、Web ページ、ファイルは外部からの入力です。送信元が ZCode のリンクとして表示されていても、それだけで信頼できる指示とみなすことはできません。また、その内容の著作権の許諾を得たことにもなりません。 |

Web ページ、リポジトリの内容、ツールの戻り値、第三者の指示には、モデルに追加の操作をさせようとする内容が含まれていることがあります。信頼できないプロジェクトを扱うときは、実行するアカウント、認証情報、ネットワークの権限を制限し、重要な操作を確認してください。

Web またはリモートのサービスを自分で配置するときは、実際の入口に合わせて、認証、通信の保護、アクセス制御を設定してください。アプリのアカウントでのログインは、配置の層で設ける安全の境界の代わりにはなりません。Web の起動パラメータと認証の設定は [README の実行方法](README.ja.md)を参照してください。独立したリモートサービスのコアは、ローカルのループバックアドレスでの待ち受けだけを受け付け、同等の外部アカウント認証は提供していません。HTTP／WS と HTTPS／WSS、SSH では、保護の条件が異なります。すべての配置が既定で暗号化されることは保証しません。また、リモートのホストやサービスの運営者が、そこで処理されるデータにアクセスできないことも保証しません。リモートのコアの制限は [待ち受けのルール](packages/zcode-server-cli/src/server-core/http.ts#L115)を参照してください。

## 二、アップロードの窓口、外部への要求、業務上の用途

| 業務の場面 | 業務上の用途、実行される条件、データの範囲 |
| --- | --- |
| モデルと補助モデルへの要求 | タスクの実行、圧縮、タイトル、記憶、過去のセッションの抽出、目標の達成の検証、AI による Git のコミットメッセージの生成、モデルへの接続テストで、モデルを呼び出すことがあります。補助的な呼び出しの一部は、タスクに伴って自動で行われます。関連するプロンプト、履歴、コード、diff、ツールの結果、添付ファイルを送ります。具体的な範囲は機能によって異なり、接続テストでは決まった確認用のメッセージを使います。要求には、認証情報、クライアントの環境、セッション／要求／追跡の識別子が付くこともあります。Anthropic 互換の呼び出しでは、機器とセッションの識別子を付け加えます。受け取る相手は、実際のエンドポイント、下記のゲートウェイ転送、プロキシ設定によって決まります。SDK の名前だけで判断することはできません。 |
| ログイン、認可、アカウント、API の認証情報 | ログイン、認可コードの交換、ログイン状態の定期確認（ポーリング）、アカウントと利用権の照会では、認可の状態、code、アクセストークン、ポーリング用の認証情報、クライアントの識別子を送ることがあります。ログインしたあと、またはアカウント設定を解析するときに、プロジェクトの API Key を自動で照会、取得、または作成することがあります。サブスクリプション済みのチームのプロジェクトでも、料金情報の更新の中で、足りない Key を作成することがあります。内蔵の料金プランのページでは、接続元の確認を通ったページにログインの認証情報、言語、テーマを渡し、機器の識別子、アカウントの識別子、アプリのバージョンを提供することがあります。現在の内蔵のアカウントアダプタは、アカウントのリフレッシュトークンの交換を実装していません。MCP OAuth の自動更新については「MCP の接続、OAuth、ツールの呼び出し」を参照してください。アカウントの認証はアクセスする人の身元を示すものであり、その後の送信の一つ一つに、ユーザーの個別の確認があることを意味しません。外部の認可 Web ページの内部の処理は、このリポジトリでは確認していません。 |
| 料金プラン、注文、支払い、利用枠 | 利用枠のリセットと関連する状態の同期では、アカウントの認証情報、個人またはチーム／プロジェクトの範囲、リセットの種類、重複防止の識別子を業務サービスに送ることがあります。機会の要求、状態のポーリング、既読の処理は自動で行われることがあります。このリポジトリには、登録済みの注文の試算／作成／取消、更新（継続購入）、支払い方法の管理、支払いのサービスがあります。呼び出すと、製品、注文、支払い方法または認可の識別子、金額、サブスクリプションの選択肢、呼び出し側が指定した経路や追跡のための項目を送ることがあります。サービスのメソッドがあることは、どの配布形態にも使える購入画面があることを意味しません。また、ページを開くだけで支払いが始まることも意味しません。 |
| 公式 Coding Plan のモデルゲートウェイへの転送 | 現在のコードに記載されている二つの公式 Anthropic 互換モデルのエンドポイントについて、プロトコル、ホスト、実際のポート、パスで一致を判定します。一致すると、自動で ZCode のゲートウェイに送り先を変えます。このとき、要求のメソッド、本文、クエリパラメータ、Host 以外の要求ヘッダーをそのまま保ちます。要求に含まれる認証情報も含みます。実際のゲートウェイの origin は、ZCODE_BASE_URL／ZCODE_ENDPOINT_ORIGIN の設定で変えられます。この転送には、ユーザーによる追加の個別確認はありません。判定の基準は要求の URL です。プロバイダをユーザー自身が作成したからといって、必ずゲートウェイを通らないと推定することはできません。ゲートウェイが要求を受け取ったあとの内部の処理は、クライアントのソースコードで確認した範囲には含まれません。 |
| プロバイダ、業務設定、接続の確認 | 起動後のバックグラウンドの確認、設定の更新、サービスの確認、関連するページでの操作によって、プロバイダの一覧、業務設定、ヘルプの内容、CDN にアクセスすることがあります。このとき、要求 URL、バージョン、プラットフォーム、窓口ごとに組み立てた接続元の情報を送ります。ヘルプ設定の要求には機器の識別子が付くこともあり、サービスの接続確認には設定された認証ヘッダーが付くこともあります。プロバイダ一覧のダウンロードと公開設定の読み取りは、管理側の認証を引き継ぎません。一部の要求はモデルのタスクとは別に動き、毎回の手動の更新を必要としません。モデルの接続テストは、モデルへの要求の経路を別に通ります。確認用のサービスのメソッドがあることは、すべての確認が起動時に自動で行われることを意味しません。 |
| 検索、Web ページの処理、リモートのリソース | モデルのツール、ユーザーによるページへのアクセス、リソースの表示によって、モデルのプロバイダ、対象のサイト、リソースのサービスに、検索語、URL、要求ヘッダー、通常のネットワーク情報を送ることがあります。現在の WebSearch は、ネイティブの検索に対応するモデルのプロバイダを通じて実行します。WebFetch で取得した内容は、処理のためのプロンプトと一緒にモデルに送られることもあります。Web ページのダウンロードを、その後はローカルだけで処理されるものと理解してはいけません。Web ページ、プラグインマーケットの画像、その他のリモートのリソースの受け取り側は、アクセスの要求と IP などの情報を得ることがあります。 |
| MCP の接続、OAuth、ツールの呼び出し | MCP を有効にし、ランタイムが対応する設定を読み込むと、ワークスペースやプラグインが宣言したサービスに自動で接続し、プロトコルを取り決め、ツールを見つけることがあります。OAuth では、探索、クライアントの登録、認可の交換、トークンの自動更新も行うことがあります。ツールの呼び出しでは引数を送り、ワークスペースの絶対パス、ワークスペースの識別情報、セッション／ターン／追跡の識別子、リモートセッションの識別子を自動で付け加えることがあります。HTTP/SSE は設定されたサービスに送ります。stdio はまずローカルまたは実行ホスト上のプロセスに渡し、その後のネットワーク上の動作はそのプログラムによって決まります。ツールの承認は、接続、探索、更新の手順をまとめて対象にするものではありません。 |
| SSH／WSL／コンテナ、その他の実行環境 | 接続を確立したあと、またはタスクを実行したあと、プロトコルのコマンド、プロンプト、ファイル、添付ファイル、ツールの結果、ターミナルの状態を、環境をまたいで受け渡すことがあります。個人のプロバイダ設定、アカウントの選択設定、同期を許可したアカウントの API Key、OAuth のアクセス／リフレッシュトークンなどは、環境が初めてオンラインになったとき、および接続が続いている間に関連する設定や認証情報が変わったときに、対象の環境へ自動で渡されることがあります。この同期には、認証情報ごとの個別の確認の手順はありません。SSH ではネットワークを通じた転送が生じることがあり、WSL／コンテナではファイルのコピーやプロセスへの入力を使うこともあります。これらの転送の送り先は、必ずしも公式のクラウドではありません。 |
| Skills、MCP、プラグイン、設定の同期 | 設定画面で選んで同期すると、スキルやプラグインのディレクトリ、補助ファイル、マーケットの提供元、設定を対象の環境へ送ることがあります。サービスの窓口は、呼び出し側から直接呼ぶこともできます。スキルの同期には、選んだディレクトリの中のファイルが含まれることがあります。MCP 設定には、コマンドの引数、環境変数、要求ヘッダー、認証情報、ローカルのパスが含まれることがあります。プラグインの一部の選択肢には機密項目の絞り込みがありますが、ディレクトリのアーカイブやすべての設定に対して、秘密情報を完全に検出するものではありません。 |
| 添付ファイルの転送とメディアのプレビュー | ファイル、画像、テキストは、選択、ドラッグ＆ドロップ、貼り付け、タスクで生じた内容から来ることがあります。添付ファイルを入力欄に加え、実行セッションがある状態になると、メッセージの送信ボタンを押す前に事前の転送が始まることがあります。セッションを再開したときも、待ち行列の処理が続くことがあります。ローカルのファイルのパスは、アップロードせずに直接参照することがあります。プロトコルによる分割アップロードやリモートへの事前転送の受け取り側は、実行ホストです。リモートのメディアのプレビューでは、そのホストからローカルへ読み戻すこともあります。上に述べたローカルまたは環境をまたぐ処理は、一般への公開とは違います。ただし、その内容はその後もタスクに従ってモデルや共有の経路に入ることがあります。 |
| セッションの共有、閲覧、取り込み | 共有の操作では、選んだセッションの行の公開用の写し（公開投影）と、ファイルとして取り出せる添付ファイルを共有サービスに送ることがあります。これには、ユーザーの文章、モデルの応答と推論の文章、ツールの入出力、関連するメタデータが含まれます。選択の段階では、まず共有サービスの機能を照会することがあります。この機能の照会では、セッションの公開投影の本文は送りません。通常の UI での公開の手順では、先に内容の開示への確認を求めます。サービス側も、呼び出し側が送った確認の時刻をまず検証し、そのあとで準備の記録を作成し、添付ファイルをアップロードし、最後に公開の確認を送ります。そのため、最終的な公開が成功する前に添付ファイルが送られていることがあります。ただし、準備とアップロードの手順は、サービスによる開示の検証のあとにあります。現在の UI は、既定で公開かつ取り込み可能です。閲覧と取り込みは、公開のチェックを前提としません。確認の時刻の項目は、どの呼び出し側も UI でチェックを入れたことの証明にはなりません。閲覧と取り込みでは、共有コードと使える認証情報を送り、添付ファイルのダウンロード先にアクセスします。取り込みでは、要求の識別子も送ります。公開投影の絞り込みと文章の処理は、秘密情報を完全に検出するものではありません。ローカルで削除や取り消しをしても、外部にすでにアップロードされた写しや、受け取った人の写しが削除されることは保証しません。 |
| 空き時間タスクの整理券と状態の同期 | 利用可否の照会、整理券を取って並ぶこと、整理券の状態の同期、使用済みの処理では、タスクの識別子、整理券の識別子、送信元／要求の識別子、Coding Plan の身元、認証情報を送ります。処理待ちの整理券があるなどの条件のときは、バックグラウンドの同期が要求を続けることがあります。これらの要求は、タスクのプロンプト全体を送ることとは違います。実際のモデルへの内容は、モデルへの要求の経路を別に通ります。 |
| 自発的なフィードバック、追加のメッセージ、診断用の添付ファイル | ユーザーがフィードバックのフォームで送信すると、本文、連絡先、アカウントとクライアントの情報、画面の画像、ファイル名を送ることがあります。サービスには、追加のメッセージと添付ファイルをアップロードする窓口が今もあります。バックグラウンドのタスクも、メッセージの窓口を使ってアップロードの結果を報告します。窓口があることは、今の画面にあとから内容を追加・編集する入口があることを意味しません。問い合わせ（チケット）の作成に成功するとフォームを閉じることがあり、送信済みの画面の画像とチェックを入れたログは、バックグラウンドのタスクがアップロードを続けます。チケットの作成が成功する前にフォームを閉じると、作成の取り消しを要求します。添付ファイルは、まずフィードバックのサービスにアップロード用の認証情報を求め、次に返されたオブジェクトストレージのアドレスへ直接ファイルを送ります。デスクトップのログの選択肢は、既定ではチェックが入っていません。チェックを入れると、ローカルでのその日の更新時刻と保管のルールに従って診断用のログファイルを選び、ファイル全体を処理します。その日に生じた記録だけが含まれることは保証しません。ログにチェックを入れなくても、本文やその他の添付ファイルには影響しません。文章の処理のルールも、画面の画像や任意のファイルから個人情報や機密が取り除かれることを保証しません。 |
| インストール、プラグインの取得、リモートへの配置、更新 | インストール、探索、認証、配置、更新では、リポジトリ、パッケージのサービス、プラグインマーケット、CDN、更新サーバーにアクセスします。統一の CLI インストールスクリプトは、設定された配布元からバージョンの一覧を読み、配布パッケージをダウンロードします。デスクトップの自動更新は、配布形態と実行の条件によって制御されます。有効にすると、起動時と定期的な確認のときに更新の一覧を要求し、プラットフォーム、チャネル、機器などの情報を送ります。プラットフォームの値はアーキテクチャを区別でき、バージョンの情報も更新の選び方に影響します。パッケージ版は、`ZCODE_UPDATE_FEED_URL` 環境変数と `--zcode-update-feed-url` 起動パラメータによる更新元の上書きを無視します。この二つの上書きの入口は、開発用のビルドだけのものです。ダウンロードしたコードは、その後インストールまたは実行されることがあります。リモートへの配置では、ローカルにすでにあるコンポーネントや、ダウンロードしたコンポーネントを、対象の環境へアップロード／コピーすることもあります。ダウンロードの確認、実際のインストール、コンポーネントの実行は、それぞれ制御の条件が異なります。すべての段階で先に確認の画面が出る、とまとめて説明することはできません。 |
| ツール、Hook、ワークフロー、自動タスクによる引き渡しや公開 | Shell、ターミナル、Git、Node REPL、ブラウザ、プラグイン、MCP、ワークフローは、タスクに関わるデータを読み取り、対応する送り先のサービスへファイルや生成物をアップロードし、外部サービスの状態を変えることがあります。ライフサイクル Hook と設定済みの定時／バックグラウンドタスクも、プログラムを起動することがあります。実行の範囲は、権限ルール、モード、プラグイン設定、システムのアカウントによって決まります。外部に出るすべての内容をユーザーが一つずつ選ぶこと、また外部への送信のたびに個別の確認があることは保証しません。公開、送信、購入、配置が完了したあとは、ローカルで取り消しや撤回をしても、外部の状態が元に戻るとは限りません。権限の範囲は第二節を参照してください。 |
| 開発時のテスト、デバッグ、要求の記録 | 関連する開発ツールを明示的に実行すると、設定された実際のモデルやツールのサービスへ、テスト用のプロンプト、サンプルの資料、認証ヘッダーを送ることがあります。実際の上流に向けて設定した記録用のプロキシは、要求を転送することがあります。名前に「テスト」や「記録」が含まれていても、完全にオフラインだとは言えません。要求の履歴やデバッグ用のファイルには、モデルの内容がローカルに保存されることもあります。これらのツールを起動する入口は、通常の製品の実行とは異なります。 |
| ビルド、署名、保守担当者による公開 | インストール、ビルド、公開のスクリプトを明示的に実行すると、依存関係、ネイティブのバイナリ、ランタイムをダウンロードし、ビルドの素材を公開用の共有ディレクトリへコピーし、Git へ push し、または署名ツールや Apple の公証サービスに処理を渡すことがあります。外部の署名ツールと、別途設定した CI、ストレージ、配布のサービスには、それぞれ独自のネットワーク上の動作があります。このリポジトリで詳しく示していない手順を、スクリプトの名前だけで確認済みと推定することはできません。関連する呼び出しは、起動の入口、環境、必要な認証情報がそれぞれ異なります。CLI SEA の共有ディレクトリへの公開とアップロードでは、先に第三者の表示資料を検証し、検証に失敗するとその公開の手順を止めます。現在の native-search の準備の手順では、対象のプラットフォームごとに、リポジトリに内蔵されたアーカイブを検証して展開します。この手順ではそれらのアーカイブをダウンロードしません。Node などその他のコンポーネントには、それぞれ別のダウンロードの経路があります。 |

上に述べた業務上の用途は、このプロジェクトが提供する窓口についてのものです。ユーザーが自分で書いたスクリプト、設定した任意のアドレス、別途インストールした第三者のコンポーネントは、追加のデータ送信を行うことがあります。ZCode から起動されたというだけで、その用途や受け取る相手が公式の審査を受けていると推定することはできません。

## 三、ローカルのデータ、ログ、認証情報

データは、デスクトップの機器、ブラウザ、CLI が動いているホスト、SSH／WSL の環境、外部サービスに、それぞれ別々に保存されることがあります。デスクトップとサーバー側のデータディレクトリは、実行環境と起動設定の影響を受けることがあり、設定、セッション、添付ファイル、キャッシュ、タスクの記録、プラグイン、ログを含みます。独立した CLI の既定のセッションデータベースは `~/.zcode/cli/db/db.sqlite` にあります。ウインドウを閉じる、ログアウトする、クライアントをアンインストールする、一つのワークスペースを削除する、といった操作で、他の環境、独立したデータディレクトリ、バックアップ、外部サービスにあるすべての写しが消去されたと理解してはいけません。

記憶機能は、既定値と入口によって扱いが異なります。独立した CLI の既定の設定では記憶が有効で、デスクトップアプリの設定では既定で無効です。有効にすると、保存済みのセッションを分析し、記憶を保存し、その後のタスクで再利用することがあります。関連する抽出や整理では、追加のモデルへの要求が生じることがあります。自動化の計画、実行の記録、未完了のタスクにも、それぞれ別の保存場所があります。これらの記録を復元しても、以前の外部への操作が取り消されること、または完全にやり直せることは保証しません。

共有 Agent のモデルの入出力ログは、開発と本番の実行では、既定でローカルに書き込まれます。テスト環境は除きます。ログには、プロンプト、コード、コンテキスト、ツールの引数、モデルの応答が含まれることがあります。要求ヘッダーと一部の画像・動画のデータには伏せ字の処理をしていますが、ユーザーの文章がすべて伏せ字になっているとみなすことはできません。本番では既定でログの切り替え（ローテーション）と切り詰めを行いますが、完全な保存を明示的に有効にするとこの動作が変わります。デスクトップのメインプロセス、Host、スケジューラ、Web サービス、リモートの常駐プロセスにも、それぞれ別のログとクラッシュの記録があります。一つのログの上限は、プロジェクト全体に共通する保存期間を表すものではありません。

認証情報の保存の仕組みも、それぞれ異なります。CLI の共有の認証情報ファイルは暗号化の仕組みを使いますが、既定の鍵はローカルの環境の情報から作られることがあり、読み取りの処理は平文の記録も受け付けます。デスクトップ／Node のサービスの認証情報も、ローカルの暗号化ファイルと、設定できる鍵または環境から作られる鍵を使います。システムのキーチェーンではありません。Web の OAuth の認証情報は localStorage に保存されることがあります。モデルの設定、環境変数、プラグイン／MCP の設定、SSH の設定、リモートと同期したデータにも、有効な認証情報が含まれることがあります。これらの保存をまとめて、システムのキーチェーン、ハードウェアによる分離、「常にローカルから出ない」ことの保証とみなしてはいけません。

内蔵ブラウザは、永続的な閲覧データを使います。他のブラウザの Cookies と LocalStorage を明示的に取り込むと、対応するログイン状態を得ることがあります。キャッシュの消去と、すべてのサイトデータの消去は、別の操作です。ブラウザの記録、ダウンロードしたファイル、Web ページの録画の成果物、モデルのログも、同じ消去の入口で削除されるとは限りません。本物の鍵や token を、バージョン管理で追跡しているファイルに書かないでください。ログのコピー、フィードバックのアップロード、会話の共有、データディレクトリのバックアップを行う前に、受け取る範囲を確認してください。本物の認証情報、元のセッション、非公開のプロジェクトの内容を、公開リポジトリや問題報告に出さないようにしてください。

## 四、第三者のライセンスと著作権の表示

このリポジトリの自社（第一者）のコードは、ルートの [LICENSE](LICENSE) に従い Apache-2.0 を採用しています。このライセンスは、他の権利者に代わって新たな許諾を与えるものではありません。また、第三者のソフトウェア、複製したコード、ネイティブのバイナリ、フォント、アイコン、Web の素材、その他のリソースに付いている個別の条件を上書きしません。具体的な依存パッケージは [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) を参照してください。第三者の著作権、ライセンス、再配布の条件などの制約があるため、公式の製品のすべての機能とキャンペーンの方針を提供することは約束しません。具体的な内容は、実際に公開したソースコードとビルド成果物によります。
